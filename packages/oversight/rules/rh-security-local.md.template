---
description: "User-specific private directory declarations — generated by rh-oversight init"
---

# Security Rules (Local)

# Add your private directories here. Uncomment and customize:
# - `Personal/` is PRIVATE. Contents must never appear in any agent output, summary, or artifact.
# - `Financial/` is PRIVATE.
